name: pin_ui
description: "A set of all necessary widgets for implementing PIN code feature screens in Flutter applications"
version: 0.0.1
repository: https://github.com/AndreySosnovyy/flutter_pin_code_ui
issue_tracker: https://github.com/AndreySosnovyy/flutter_pin_code_ui/issues
topics:
  - pin
  - pin-code
  - widgets

environment:
  sdk: ^3.5.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
